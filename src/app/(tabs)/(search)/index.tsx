import { EmptyState } from '@/components/ui';

export default function SearchScreen() {
  return (
    <EmptyState
      icon="🔍"
      title="Search the catalog"
      message="Product search and filters connect to the catalog API in Phase 2. The search bar in the header is ready."
    />
  );
}
